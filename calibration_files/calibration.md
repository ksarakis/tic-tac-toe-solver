# How to calibrate this arm

```bash
lerobot-calibrate \
 --robot.type=so101_follower \
 --robot.port=/dev/ttyACM0
 --robot.id=follower-1 # robot-name
```

* Move the calibration file to `calibration_files/`